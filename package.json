{
  "name": "fabrika-polo",
  "version": "1.0.0",
  "description": "Сайт компании Фабрика Поло",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_BASE_PATH=/fabricapolo/ vite build && cp dist/index.html dist/404.html && (rm -rf node_modules/.cache/gh-pages 2>/dev/null; git branch -D gh-pages 2>/dev/null; true) && npx gh-pages -d dist -f"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.0",
    "vite": "^5.0.0",
    "vite-plugin-image-optimizer": "^2.0.3",
    "sharp": "^0.34.0",
    "gh-pages": "^6.1.1"
  }
}
